import { requireManager } from "@/lib/auth"; import { db } from "@/lib/db"; import { ActionForm } from "@/components/workspace/action-form"; import { createProgrammeSessionAction, assignSessionPeopleAction } from "@/app/actions/programme";
export default async function ProgrammePage(){const u=await requireManager();const assigned=u.role==="ADMINISTRATOR"?undefined:{some:{userId:u.id,active:true}};const cs=await db.conference.findMany({where:assigned?{managerAssignments:assigned}:undefined,include:{programmeDays:true,rooms:true,programmeSessions:{include:{day:true,room:true},orderBy:{startsAt:"asc"}}}});const c=cs[0];if(!c)return <p className="empty-state">No assigned conferences.</p>;return <><header className="admin-header"><h1>Programme scheduling</h1></header><section className="admin-panel"><ActionForm action={createProgrammeSessionAction} submitLabel="Create session" className="grid2"><input type="hidden" name="conferenceId" value={c.id}/><label>Title<input name="title" required/></label><label>Day<select name="dayId">{c.programmeDays.map(d=><option key={d.id} value={d.id}>{d.title}</option>)}</select></label><label>Type<select name="type"><option>KEYNOTE</option><option>PLENARY</option><option>ORAL</option><option>POSTER</option><option>WORKSHOP</option><option>PANEL</option></select></label><label>Start<input name="startsAt" type="datetime-local" required/></label><label>End<input name="endsAt" type="datetime-local" required/></label></ActionForm></section><section className="admin-panel"><h2>Sessions</h2>{c.programmeSessions.map(s=><article key={s.id}><p><strong>{s.title}</strong> — {s.day.title} · {s.room?.name||"Online"}</p><ActionForm action={assignSessionPeopleAction} submitLabel="Save chair/moderator" className="inline-form"><input type="hidden" name="conferenceId" value={c.id}/><input type="hidden" name="sessionId" value={s.id}/><input name="chairName" defaultValue={s.chairName||""} placeholder="Chair / co-chair"/><input name="moderatorName" defaultValue={s.moderatorName||""} placeholder="Moderator"/></ActionForm></article>)}</section></>}
