import { PageHeader, publicStyles as styles } from "@/components/public/PublicUI";
import { AccountLoginForm, RegistrationForm } from "@/components/workspace/account-forms";
export const metadata = { title: "Login" };
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ returnTo?: string }> }) { const query = await searchParams; return <><PageHeader eyebrow="Account access" title="Register / Login">Submit your work, track decisions, and complete assigned peer reviews in one secure workspace.</PageHeader><div className={`${styles.container} ${styles.content}`}><div className={styles.grid2}><section className={styles.card}><h2>Sign in</h2><AccountLoginForm returnTo={query.returnTo}/></section><section className={styles.card}><h2>Create an account</h2><RegistrationForm/></section></div><p>Conference staff can use the same sign-in form. Platform administrators may also use the dedicated <a href="/admin/login">administrator login</a>.</p></div></>; }
