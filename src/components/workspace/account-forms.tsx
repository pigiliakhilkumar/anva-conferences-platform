"use client";
import { useActionState } from "react";
import { accountLoginAction, registerAction } from "@/app/actions/account";
const initial = { ok: false, message: "" };
export function AccountLoginForm({ returnTo }: { returnTo?: string }) { const [state, action, pending] = useActionState(accountLoginAction, initial); return <form action={action} className="auth-form"><input type="hidden" name="returnTo" value={returnTo || "/workspace"}/><label>Email<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={10} required/></label>{state.message && <p className="notice error" role="alert">{state.message}</p>}<button className="primary" disabled={pending}>{pending ? "Signing in…" : "Sign in"}</button></form>; }
export function RegistrationForm() { const [state, action, pending] = useActionState(registerAction, initial); return <form action={action} className="auth-form"><label>Full name<input name="name" autoComplete="name" required minLength={2}/></label><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="new-password" minLength={12} required aria-describedby="password-help"/></label><small id="password-help">At least 12 characters with upper- and lowercase letters, a number, and a symbol.</small><label>Confirm password<input name="confirmPassword" type="password" autoComplete="new-password" required/></label>{state.message && <p className="notice error" role="alert">{state.message}</p>}<button className="primary" disabled={pending}>{pending ? "Creating account…" : "Create participant account"}</button></form>; }
